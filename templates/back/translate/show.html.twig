{% extends 'base.html.twig' %}

{% block title %}Translate{% endblock %}

{% block body %}
    <h1>Translate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ translate.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ translate.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_translate_index') }}">back to list</a>

    <a href="{{ path('app_back_translate_edit', {'id': translate.id}) }}">edit</a>

    {{ include('back/translate/_delete_form.html.twig') }}
{% endblock %}
